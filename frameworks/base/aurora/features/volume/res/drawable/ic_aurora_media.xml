<?xml version="1.0" encoding="utf-8"?>
<!--
     Aurora's own icon, not AOSP's.

     SystemUI's volume icons are @hide resources belonging to the dialog Aurora replaced, and
     reaching for them would tie this surface's appearance to a component that is no longer running.
     A vector in the plugin's own res/ is self-contained, ships in the same APK, and is resolved
     through pluginContext rather than sysuiContext.

     Media: a note rather than a speaker, because every stream is "volume" and only one of them is
     music. The point of the icon is telling the streams apart.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24"
    android:tint="#FFFFFFFF">
    <path
        android:fillColor="#FF000000"
        android:pathData="M12,3v10.55c-0.59,-0.34 -1.27,-0.55 -2,-0.55 -2.21,0 -4,1.79 -4,4s1.79,4 4,4 4,-1.79 4,-4V7h4V3h-6z" />
</vector>
